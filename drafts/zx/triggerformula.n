//--------------------
#general trigger throttle functions, fancy

gas = val - rig  //less than 0 till val=rig

gas = rig - val  //begin at rig until 0. when val = rig

gas = Math.sqrt(gas) 

cos(pi*x) / (-LN2*x);
r(t) = 3;
d(d(x^4 / 12 - 3/2 x^2));
(x^3)/3 - 1.5 x^2 + x;
pi*asin(t/2 - 0.25pi);

x;
((x^0.5) + (x^2)*3) /4;
((x^2));
((x^0.5) + (x)) /2;

1-(abs(x-0.5)*2);
(1+1-(abs(x-0.5)*2)+1-(abs(x-1.0)*2))*0.5;
(1-(abs(x-0.5)*2)+1-(abs(x-.75)*2)+1-(abs(x-1.0)*2))*0.3333;
